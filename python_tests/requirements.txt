# Python test dependencies for RpcNet bindings

# Core testing framework
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Build tool (optional, but recommended)
maturin>=1.0.0

# Optional: Code coverage
pytest-cov>=4.0.0

# Optional: Parallel test execution
pytest-xdist>=3.0.0

# Optional: Better output formatting
pytest-sugar>=0.9.0
